---
const { content } = Astro.props;
import Nav from "../components/Nav.svelte";
---

<html lang="en">
  <head>
    <title>{content.title} - ucicalendar.com</title>
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta charset="utf-8" />
    <meta name="description" content={content.description} />
    <meta property="og:title" content={content.title + " - ucicalendar.com"} />
    <meta property="og:description" content={content.description} />

    <script src="https://cdn.usefathom.com/script.js" data-site="HAMJDXVG" defer
    >

    </script>
  </head>

  <!-- Permanently set dark-theme class so Radix dark scales apply when prefers-color-scheme is dark. -->
  <body class="dark-theme">
    <div id="nav-parent">
      <Nav pathname={Astro.url.pathname} />
    </div>
    <main>
      <article>
        <slot />
      </article>
    </main>

    <style is:global>
      @import "@radix-ui/colors/gray.css";
      @import "@radix-ui/colors/blue.css";
      @import "@radix-ui/colors/red.css";

      @import "@radix-ui/colors/gray-dark.css" (prefers-color-scheme: dark);
      @import "@radix-ui/colors/blue-dark.css" (prefers-color-scheme: dark);
      @import "@radix-ui/colors/red-dark.css" (prefers-color-scheme: dark);

      body {
        margin: auto;
        padding: 0 16px;
        max-width: 600px;
        color: var(--gray-12);
        font-family: sans-serif;
        line-height: 130%;
      }

      body.dark-theme {
        background-color: var(--gray-1);
      }

      #nav-parent {
        margin-top: 1em;
      }

      main {
        margin-bottom: 8em;
      }

      h1 {
        margin-top: 2em;
      }

      h2 {
        margin-top: 1.5em;
      }

      code {
        padding: 2px;
        border-radius: 4px;
        background-color: var(--gray-3);
      }

      a {
        color: var(--blue-11);
      }
    </style>
  </body>
</html>
